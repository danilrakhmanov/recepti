# Правила безопасности Firebase Firestore

Для работы синхронизации данных между устройствами необходимо настроить правила безопасности в консоли Firebase.

## Архитектура персональных данных

Данные хранятся в структуре `users/{userId}/data/{collection}`, что обеспечивает:
- Полную изоляцию данных между пользователями
- Безопасность на уровне документа
- Легкую миграцию и масштабирование

### Структура данных:
```
users/
  {userId}/
    data/
      recipes/          # Рецепты пользователя
      weeklyMenu/       # Меню на неделю
      shoppingList/     # Список покупок
```

## Как работает авторизация

В проекте реализована Firebase Authentication с использованием Email/Password. Правила проверяют, что пользователь авторизован и имеет доступ только к своим данным.

### Процесс авторизации:

1. **Вход/Регистрация**: Пользователь вводит email и пароль через модальное окно
2. **Firebase Auth**: Firebase проверяет учетные данные и создает сессию с уникальным `uid`
3. **Правила безопасности**: Firestore проверяет `request.auth.uid` перед доступом к данным
4. **Синхронизация**: Данные синхронизируются между всеми устройствами авторизованного пользователя в реальном времени

## Инструкция по настройке:

### Шаг 1: Включите Firebase Authentication

1. Откройте консоль Firebase: https://console.firebase.google.com/
2. Выберите ваш проект: `recepti-93f83`
3. Перейдите в раздел: Authentication -> Sign-in method
4. Нажмите "Добавить новый провайдер"

#### Включите Email/Password:
1. Выберите "Email/Password"
2. Включите провайдер и нажмите "Сохранить"

#### Включите Google:
1. Выберите "Google"
2. Включите провайдер
3. **Важно**: Добавьте ваш домен в "Authorized domains"
   - Для локальной разработки: добавьте `http://localhost:3000` или `http://127.0.0.1:5500`
   - Для GitHub Pages: добавьте `https://ваш-username.github.io/recepti`
   - Для Netlify/Vercel: добавьте ваш домен (например, `https://ваш-проект.netlify.app`)
   - Для собственного домена: добавьте ваш домен (например, `https://ваш-сайт.com`)
4. Нажмите "Сохранить"

### Шаг 2: Настройте Firestore для персональных данных

1. Перейдите в раздел: Firestore Database -> Rules
2. Замените текущие правила на следующие:

```firestore
rules_version = '2';
service cloud.firestore {
  match /databases/{database}/documents {
    
    // Личные данные пользователя (рецепты, меню, список покупок)
    match /users/{userId}/data/{document=**} {
      allow read, write: if request.auth != null && request.auth.uid == userId;
    }
    
    // Публичные рецепты (опционально)
    match /publicRecipes/{documentId} {
      allow read: if true;
      allow write: if request.auth != null;
    }
    
  }
}
```

## Как работает синхронизация:

1. **При входе**: 
   - Данные загружаются с сервера в структуру `users/{userId}/data/`
   - Если на устройстве есть локальные данные, они загружаются на сервер

2. **В реальном времени**:
   - `onSnapshot` listener отслеживает изменения на сервере
   - При изменении данных на другом устройстве, они автоматически обновляются

3. **При изменении**:
   - Данные сохраняются в localStorage (локально)
   - Данные отправляются на сервер в `users/{userId}/data/`
   - Все устройства пользователя получают уведомление об изменении

## Пример структуры данных в Firestore:

```
users/
  abc123xyz/
    data/
      recipes/
        recipe1: {
          name: "Борщ",
          ingredients: [...],
          steps: [...],
          userId: "abc123xyz",
          updatedAt: timestamp
        }
      weeklyMenu/
        monday: { breakfast: "Омлет", lunch: "Борщ", dinner: "Чай" }
      shoppingList/
        item1: { name: "Свекла", checked: false }
```

## Преимущества такого подхода:

- ✅ **Безопасность**: Данные изолированы на уровне правил
- ✅ **Производительность**: Запросы не требуют индексов по полю userId
- ✅ **Масштабирование**: Легко добавлять новые типы данных
- ✅ **Резервное копирование**: Можно легко экспортировать данные пользователя
- ✅ **Real-time sync**: Изменения мгновенно синхронизируются между устройствами

## Как использовать авторизацию в приложении:

1. Нажмите на кнопку 👤 в правом верхнем углу
2. Выберите вкладку "Регистрация" для создания аккаунта или "Вход" для входа
3. Введите email и пароль (минимум 6 символов)
4. После успешной авторизации:
   - Данные синхронизируются с сервером
   - При входе с другого устройства вы получите те же данные
   - Все изменения сохраняются и доступны на всех устройствах
