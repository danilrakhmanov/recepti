# Правила безопасности Firebase Firestore

Для работы синхронизации данных между устройствами необходимо настроить правила безопасности в консоли Firebase.

## Как работает авторизация

В проекте реализована Firebase Authentication с использованием Email/Password. Правила `request.auth != null` проверяют, что пользователь авторизован перед доступом к данным.

### Процесс авторизации:

1. **Вход/Регистрация**: Пользователь вводит email и пароль через модальное окно
2. **Firebase Auth**: Firebase проверяет учетные данные и создает сессию
3. **Правила безопасности**: Firestore проверяет `request.auth != null` перед доступом к данным
4. **Синхронизация**: Данные синхронизируются между всеми устройствами авторизованного пользователя

## Инструкция по настройке:

### Шаг 1: Включите Firebase Authentication

1. Откройте консоль Firebase: https://console.firebase.google.com/
2. Выберите ваш проект: `recepti-93f83`
3. Перейдите в раздел: Authentication -> Sign-in method
4. Нажмите "Добавить новый провайдер"

#### Включите Email/Password:
1. Выберите "Email/Password"
2. Включите провайдер и нажмите "Сохранить"

#### Включите Google:
1. Выберите "Google"
2. Включите провайдер
3. **Важно**: Добавьте ваш домен в "Authorized domains"
   - Для локальной разработки: добавьте `http://localhost:3000` или `http://127.0.0.1:5500`
   - Для GitHub Pages: добавьте `https://ваш-username.github.io/recepti`
   - Для Netlify/Vercel: добавьте ваш домен (например, `https://ваш-проект.netlify.app`)
   - Для собственного домена: добавьте ваш домен (например, `https://ваш-сайт.com`)
4. Нажмите "Сохранить"

**Если вы видите ошибку "This domain is not authorized for OAuth operations"**:
- Убедитесь, что ваш домен добавлен в "Authorized domains" в настройках Google Sign-in
- Проверьте, что вы используете правильный протокол (http или https)
- Для локальной разработки используйте `http://localhost:порт`

**Важно: SHA-1 Fingerprint**
- Для Android приложений: SHA-1 fingerprint добавляется автоматически
- Для веб-приложений: SHA-1 fingerprint не требуется для Google входа
- Если вы видите ошибку о SHA-1 fingerprint, игнорируйте её для веб-приложения

#### Включите Phone:
1. Выберите "Phone"
2. Включите провайдер
3. Выберите метод проверки: "SMS"
4. Добавьте номер телефона для тестирования (опционально)
5. **Важно**: Убедитесь, что биллинг включен в вашем проекте Firebase
6. Нажмите "Сохранить"

**Если вы видите ошибку "auth/billing-not-enabled"**:
- Это означает, что биллинг не включен в вашем проекте Firebase
- Для бесплатного плана Spark: Phone Auth не доступен
- Вам нужно перейти на платный план Blaze для использования Phone Auth
- Или используйте только Email и Google авторизацию

### Шаг 2: Настройте правила Firestore

1. Перейдите в раздел: Firestore Database -> Rules
2. Замените текущие правила на следующие:

```firestore
rules_version = '2';
service cloud.firestore {
  match /databases/{database}/documents {
    
    // Рецепты - чтение и запись для всех авторизованных пользователей
    match /recipes/{recipeId} {
      allow read, write: if request.auth != null;
    }
    
    // Список покупок - чтение и запись для всех авторизованных пользователей
    match /shoppingList/{itemId} {
      allow read, write: if request.auth != null;
    }
    
    // Настройки (меню) - чтение и запись для всех авторизованных пользователей
    match /settings/{documentId} {
      allow read, write: if request.auth != null;
    }
    
  }
}
```

## Вариант для тестирования (без авторизации):

Если вы хотите протестировать синхронизацию без настройки аутентификации, используйте эти правила (только для разработки!):

```firestore
rules_version = '2';
service cloud.firestore {
  match /databases/{database}/documents {
    
    // Разрешить чтение и запись всем (только для тестирования!)
    match /{document=**} {
      allow read, write: if true;
    }
    
  }
}
```

## Рекомендации по безопасности:

Для продакшн-версии рекомендуется:
1. ✅ Настроить Firebase Authentication (Email/Password)
2. ✅ Ограничить доступ к данным только для авторизованных пользователей
3. ✅ Добавить валидацию данных
4. ✅ Использовать пользовательские утверждения (claims) для управления доступом

## Пример правил с валидацией данных:

```firestore
rules_version = '2';
service cloud.firestore {
  match /databases/{database}/documents {
    
    // Рецепты с валидацией
    match /recipes/{recipeId} {
      allow read, write: if request.auth != null
        && request.resource.data.name is string
        && request.resource.data.name.size() > 0
        && request.resource.data.mealType in ['first', 'second', 'salads', 'snacks', 'baking', 'dessert'];
    }
    
    // Список покупок с валидацией
    match /shoppingList/{itemId} {
      allow read, write: if request.auth != null
        && request.resource.data.name is string
        && request.resource.data.name.size() > 0;
    }
    
    // Настройки (меню)
    match /settings/{documentId} {
      allow read, write: if request.auth != null;
    }
    
  }
}
```

## Как использовать авторизацию в приложении:

1. Нажмите на кнопку 👤 в правом верхнем углу
2. Выберите вкладку "Регистрация" для создания аккаунта или "Вход" для входа
3. Введите email и пароль (минимум 6 символов)
4. После успешной авторизации данные начнут синхронизироваться

**Важно**: Все пользователи с одним и тем же email будут видеть одни и те же данные. Если вы хотите, чтобы у каждого пользователя были свои данные, нужно изменить правила безопасности.
